const config = {
    host: "localhost",
    port: 8889, // 3306 for windows
    uname: "root",
    pword: "root", // leave blank for windows
    database: "db_authoring_w7"
}

module.exports = config;